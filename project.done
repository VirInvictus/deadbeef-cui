deadbeef-cui: FINAL BLITZ complete (2026-09-15)

Shipped: v1.3.5 (tag v1.3.5 at e2f4efa; Release with the verbatim
patchnotes body and the CI-built ddb_misc_cui_GTK3.so attached; CI green
throughout). Content: the release.yml notes flag (the audit's one HIGH),
the stale-cache/callback bug pair, the hidden-marker viewer identity
(data-loss fix), the queued v1.3.5 hygiene (shutting_down atomics, guard
uniformity, strcasestr/_GNU_SOURCE, GLib-2.74 CAS removal), the
comment/contract batch, the pre-commit fix + git-level lockstep CI gate,
per-column sort persistence, the in-widget empty-state hint, the
docs-truth and prose passes, and the hygiene/removal set. Suite: 20
tests, desktop-full and CI green, ASan/UBSan clean. The live 1.10.3
smoke caught one regression in the new menu teardown before the tag
(fixed in e2f4efa, on the tag).

Full record: audit-final/deadbeef-cui/FINAL-REPORT.md ("Executed in the
final blitz") and the roadmap audit block (all boxes ticked with notes).

Reopen conditions (none agent-executable):
- Plugin-list submission re-gate: the builder-Docker verify (x86_64) is
  Brandon's session; then the submission PR needs his credentials and
  description. The in-widget hint and this release were shaped to make
  that submission strong.
- v2.0.0 framing: deferred until a feature warrants it (decided
  2026-09-12, unchanged).
- Parked features, in the audit's rank order: MAX_COLUMNS lift (or spec
  softening), pane-width persistence via exapi keyvalues, search album
  field, gtkScriptable column-config editor, Album Art View, GTK4 port
  (the recorded blocker list is the plan). Any of these reopen the
  complete-posture question first.
- Upstream items not ours to fix: serialize_to_keyvalues leak in GTKUI
  (§10.11), the tab-strip drop-target FIXME leak (the CUI_DRAG_TARGET
  comment), the startup gtk_widget_get_scale_factor critical (GTKUI-side,
  observed once at launch).
